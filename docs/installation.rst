.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install hnn

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hnn
    $ pip install hnn
